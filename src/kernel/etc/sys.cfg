-- /etc/sys.cfg
-- system configuration. don't break this.
return {
  hostname = "AxisBox",
  
  logging = {
    enable_screen_log = true, -- print kernel logs to tty during boot
    ring_buffer_size = 8192,  -- 8kb of log history. enough for a panic stacktrace.
    log_device = "/dev/ringlog",
  },
  
  security = {
    allow_guest = true,
    root_login_allowed = true,
  }
}